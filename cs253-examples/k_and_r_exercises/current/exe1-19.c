#include <stdio.h>

#define MAXLINE = 1000 //let's assume the max line is 1000 char long.

/*
 * @author Aza Tulepbergenov
 */

int main()
{
}

void reverse(char s[])
{
    int i;
    int c; //character
    for (i = MAXLINE; i >= 0 && (c=getchar()) != EOF && c!='\n'; i++)
        //TODO: Finish this.

