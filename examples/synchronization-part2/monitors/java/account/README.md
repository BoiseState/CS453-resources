Account Monitor - A simple example
----------------------------------

Demonstrates how to convert a base Java class into a simple monitor using the synchronized keyword and the default built-in monitor implementation in every Java class.