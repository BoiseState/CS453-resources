#!/bin/perl -w

# Perl sum program

use strict;
use Carp;

sub sum(@) {
    my @seq=@_;
    my $s=0;
    for my $i (@seq) {
        $s+=$i;
    }
    return $s;
}

my @seq=(5,6,1,8,3,7);
print sum(@seq)."\n";

1;

